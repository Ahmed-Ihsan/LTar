schema: spec-driven
created: 2026-07-18
goal: Harden all untrusted-input surfaces (Excel OOXML, CLI paths, MCP queries,
  web UI, JSONL) against XXE, zip-slip, XML injection, path traversal, XSS,
  CSRF, and schema-less deserialization.
