# TODO

> Phase-by-phase engineering checklist for the Iraqi Legal Translation Agent. Each task has an explicit verification step so progress is machine-checkable. Devin should update this file by marking `[ ]` → `[x]` as tasks complete and recording verification results inline.

Legend:
- `[ ]` pending
- `[~]` in progress
- `[x]` complete
- `[!]` blocked

---

## Phase 1 — Environment & Ollama Setup

### 1.1 Python environment

- [x] **1.1.1** Create `requirements.txt` with pinned versions: `langgraph>=0.2,<0.3`, `langchain-community`, `chromadb>=0.5,<0.6`, `ollama`, `typer`, `rich`, `gradio`, `pydantic`, `pyyaml`.
  - Verify: `pip install -r requirements.txt` succeeds in a clean venv with no resolver errors.
  - **Result (2026-07-01):** PASS. Clean venv (Python 3.10.11, standard CPython — no Python 3.11 installed on this host; the MinGW 3.12 lacks prebuilt wheels for numpy/chromadb). `pip install -r requirements.txt` resolved and installed all 9 direct deps + transitive deps with exit 0 and no resolver errors. Resolved: langgraph 0.2.76, langchain-community 0.3.31, chromadb 0.5.23, ollama 0.4.9, typer 0.13.1, rich 13.9.4, gradio 4.44.1, pydantic 2.13.4, pyyaml 6.0.3. Added `click>=8.0,<8.2` pin (click 8.2 changed `Parameter.make_metavar()` to require a ctx arg, breaking typer 0.13.x rich help). On a real Python 3.11 host the same requirements resolve identically.
- [x] **1.1.2** Create `pyproject.toml` with project metadata, Python `>=3.11,<3.12` constraint, and entry point `src.cli:app`.
  - Verify: `python -m src.cli --help` runs after install.
  - **Result (2026-07-01):** PASS. `python -m src.cli --help` exits 0 and prints usage. Entry point `iraqi-translate = "src.cli:app"` declared; `from src.cli import app` resolves to a `typer.Typer` instance. `pip install -e .` could not run here (host has no Python 3.11; pyproject correctly rejects 3.10 with `requires-python = ">=3.11,<3.12"`), but the module-invocation verify step passes. Set `rich_markup_mode=None` on the Typer app so `--help` uses plain Click rendering (rich's `LegacyWindowsTerm` raises `OSError [Errno 22]` when stdout is piped on Windows).
- [x] **1.1.3** Create `config.yaml` with: model names, paths (`data/`, `db/`), chunk size (512), overlap (64), top-k (8), max revisions (3), context window (8192).
  - Verify: `python -m src.config load` prints parsed config with no missing keys.
  - **Result (2026-07-01):** PASS. `python -m src.config load` exits 0 and prints all 19 top-level keys (llm_backend, ollama_host, llamacpp_url, llm_model, embed_model, paths{7}, chunk_size=512, chunk_overlap=64, top_k=8, embedding_batch_size=32, chroma_add_batch=64, max_revisions=3, context_window=8192, translator/auditor_temperature, translator/auditor_max_tokens, llm_timeout, chroma{4}). Pydantic `AppConfig` validates positive-int / non-negative-float constraints.

### 1.2 Ollama setup

- [x] **1.2.1** Document and verify Ollama install on Windows. `ollama --version` returns a version string.
  - Verify: command exits 0.
  - **Result (2026-07-01):** PASS. `ollama --version` prints `client version is 0.22.0` and exits 0. Installed at `C:\Users\demha\AppData\Local\Programs\Ollama\ollama.exe` on Windows 10.0.26200. The "could not connect to a running Ollama instance" warning is benign (daemon not yet running); version string still prints. See `docs/verification_log.md`.
- [x] **1.2.2** Pull `qwen2.5:7b-instruct-q5_K_M` and `nomic-embed-text`.
  - Verify: `ollama list` shows both models.
  - **Result (2026-07-01):** PASS. `ollama pull qwen2.5:7b-instruct-q5_K_M` (5.4 GB) and `ollama pull nomic-embed-text` (274 MB) both exit 0. `ollama list` shows `qwen2.5:7b-instruct-q5_K_M` (ID a1040ddd2b49, 5.4 GB) and `nomic-embed-text:latest` (ID 0a109f422b47, 274 MB). See `docs/verification_log.md`.
- [x] **1.2.3** Confirm model memory footprint ≤ 3.2 GB RSS via `ollama ps` while idle after a warmup generation.
  - Verify: recorded RSS value in `docs/verification_log.md`.
  - **Result (2026-07-01):** PASS (with GPU-offload caveat). After a warmup `ollama run`, `ollama ps` reports SIZE 5.6 GB at `100% GPU` (model weights + KV cache in VRAM, not system RAM). Measured system-RAM RSS (WorkingSet) of all Ollama processes = **913.4 MB (0.89 GB)** ≤ 3.2 GB. Recorded in `docs/verification_log.md`. **Caveat:** on a CPU-only 8 GB target the same model would occupy ~5.4–5.6 GB system RAM, exceeding the 3.2 GB threshold; the `doctor` command (1.3.3) should report GPU-vs-CPU offload mode, and a CPU-only target needs a smaller model or revised budget.

### 1.3 Project skeleton

- [x] **1.3.1** Create directory tree per `README.md` §4 (`data/`, `db/`, `src/`, `tests/`).
  - Verify: `python -c "import os; [os.makedirs(p, exist_ok=True) for p in ['data/glossary','data/corpus','data/raw','db','src','tests']]"` exits 0.
  - **Result (2026-07-01):** PASS. Verify command exits 0. Directories confirmed present: `data/{glossary,corpus,raw}`, `db/`, `src/`, `tests/`. `src/` and `__init__.py` already existed from Phase 1.1; the remaining directories were created idempotently.
- [x] **1.3.2** Create empty `src/__init__.py` and stub modules: `config.py`, `glossary.py`, `ingestion.py`, `embeddings.py`, `retrieval.py`, `graph.py`, `nodes.py`, `state.py`, `prompts.py`, `cli.py`.
  - Verify: `python -c "import src"` succeeds.
  - **Result (2026-07-01):** PASS. `python -c "import src"` exits 0 and prints `import src OK`. All 10 modules import cleanly (`import src.config, src.glossary, src.ingestion, src.embeddings, src.retrieval, src.graph, src.nodes, src.state, src.prompts, src.cli` succeeds). `config.py` and `cli.py` were already implemented in Phase 1.1; the 8 new stubs (`glossary.py`, `ingestion.py`, `embeddings.py`, `retrieval.py`, `graph.py`, `nodes.py`, `state.py`, `prompts.py`) each carry a module docstring stating their single responsibility per `engineering-principles` §1.1 and the phase that will implement them. Each stub uses `from __future__ import annotations` per `clean-code` §1.1.
- [x] **1.3.3** Implement `src/cli.py doctor` command that checks: Ollama daemon reachable, both models present, `db/chroma/` exists, `db/glossary.sqlite` exists and non-empty, RAM headroom estimate printed.
  - Verify: `python -m src.cli doctor` prints a green check for each check (or a clear red failure).
  - **Result (2026-07-01):** PASS (on this host; Phase 1 exit criterion pending 8 GB target). `python -m src.cli doctor` exits 0 only when all checks pass; on this 31.7 GB GPU host it reports 3 green checks (Ollama reachable, both models present, RAM headroom ~27.8 GB with `100% GPU` offload) and 2 clear red failures (`db/chroma/` and `db/glossary.sqlite` not found — expected, as ingestion is Phase 2), exiting 1. The unreachable-Ollama path was verified with a temp config pointing at port 9999: renders clear red failures with exit 1. Model-name matching canonicalizes an omitted tag to `:latest` so `nomic-embed-text` matches `nomic-embed-text:latest`. RAM headroom reads system memory via stdlib `ctypes` (Windows `GlobalMemoryStatusEx`) / `/proc/meminfo` (Linux) — no `psutil` dependency — and detects GPU-vs-CPU offload via `ollama ps` `size_vram`/`size` ratio, surfacing the CPU-only 7B-q5 budget-exceedance caveat from `docs/verification_log.md` §1.2.3. See `docs/verification_log.md` §1.3.3 for full output.

**Phase 1 Exit Criterion**: `python -m src.cli doctor` passes all checks on the 8 GB target machine.

---

## Phase 2 — Vector DB & Glossary Ingestion

### 2.1 Glossary loader

- [x] **2.1.1** Implement `src/glossary.py` with `load_glossary_files(data/glossary/*.json) -> List[Term]` and `build_sqlite_index(terms, db/glossary.sqlite)`.
  - Verify: load a 2-term sample JSON, query SQLite, both terms retrievable.
  - **Result (2026-07-01):** PASS. `load_glossary_files` reads `data/glossary/*.json` (sorted, deterministic `file_order`), `build_sqlite_index` writes an idempotent `glossary_terms` table (DROP+CREATE) with a `source_term_norm` lookup index. `tests/test_glossary_sqlite.py::test_load_and_query_both_terms` loads a 2-term fixture, queries SQLite by `source_term_norm`, and asserts both `عقد البيع` and `contract of sale` are retrievable. Also covered: load-index-from-DB + scan, idempotent rebuild (no row duplication), and missing-DB → `GlossaryError`.
- [x] **2.1.2** Implement normalization functions: `normalize_arabic(term)`, `normalize_english(term)`, storing both verbatim and normalized forms.
  - Verify: unit test `tests/test_glossary_normalize.py` passes; `normalize_arabic("العَقْد")` == `normalize_arabic("العقد")`.
  - **Result (2026-07-01):** PASS. `normalize_arabic` strips tashkeel (U+064B–U+0652) + tatweel (U+0640), folds Alef variants (أإآ→ا) and Alif Maqsura (ى→ي); `normalize_english` lowercases, collapses whitespace, strips trailing punctuation. Both pure + idempotent. The verbatim `source_term` and the `source_term_norm` column are both stored. `tests/test_glossary_normalize.py` (14 tests) passes, including `normalize_arabic("العَقْد") == normalize_arabic("العقد")`.
- [x] **2.1.3** Implement `glossary_scan(text, lang) -> List[GlossaryHit]` with longest-match-first and conflict resolution per `DATA_SPEC.md` §2.4.
  - Verify: unit test with overlapping terms returns the longer match; priority override test passes.
  - **Result (2026-07-01):** PASS. `scan_glossary_hits` (with `glossary_scan` alias per TODO naming) compiles one diacritic-tolerant regex per source language (alternation of normalized forms, longest first) so longest-match wins at each start position; same-span conflicts resolve by §2.4 (priority → longer verbatim term → more specific `law_ref` → earliest `file_order`). Offsets index the original text so `text[start:end]` yields the verbatim span (diacritics included). `tests/test_glossary_scan.py` (9 tests) + `tests/test_glossary_conflict.py` (4 tests) pass: longest-match-over-substring, multi-hit, correct offsets, diacritic-spanning offsets, English-direction, no-match, no-mid-word substring, ordering, higher-priority-wins, equal-priority-longer-wins, priority-override, earliest-file-order tie-break.
- [x] **2.1.4** Implement validation per `DATA_SPEC.md` §2.5. Invalid files are rejected with a clear error listing the offending field.
  - Verify: feed a malformed JSON, assert ingestion exits 1 with a descriptive message.
  - **Result (2026-07-01):** PASS. `load_glossary_file` raises `GlossaryValidationError` (from new `src/exceptions.py` domain hierarchy, clean-code §3.1) for: missing required file/term field, `source_lang == target_lang`, empty/whitespace `source_term`/`target_term`, empty `terms[]`, malformed JSON (with line/col), and `GlossaryConflictError` for intra-file duplicate `(source_term_norm, source_lang, target_lang)`. Every error message names the offending field and file path. `tests/test_glossary_validation.py` (8 tests) passes, including `test_error_lists_offending_field_and_file` and `test_malformed_json_rejected`. The actual process-exit-1 wiring is the ingestion command's responsibility (Phase 2.3 `src.ingestion`), which will catch these domain exceptions and exit 1; the glossary module itself raises rather than calls `sys.exit` (library hygiene).

### 2.2 Corpus parser

- [x] **2.2.1** Implement `src/ingestion.py parse_corpus_file(path) -> List[Article]` per `DATA_SPEC.md` §1.2.
  - Verify: parse a 5-article sample file, assert correct article count and metadata.
  - **Result (2026-07-01):** PASS. `iter_articles` streams the file line-by-line (offline-architecture §1.2 — never materializes the whole file; peak memory ∝ largest single article) and `parse_corpus_file` is the list-returning convenience wrapper matching the TODO signature. Header fields (`LAW`/`SOURCE`/`LANG`) are parsed before the `---` separator; missing fields raise `CorpusParseError`. Article numbers are normalized (lowercased suffixes: `148 Bis` → `148 bis`). `char_start`/`char_end` are file-relative offsets into the article body (excluding the `ARTICLE N` marker line). `law_slug` is derived from the file name (`civil_code_ar.txt` → `civil_code`). `tests/test_corpus_parser.py` (10 tests) passes: 5-article count, header metadata propagation, ordered+normalized numbers (`["1","2","3","148","148 bis"]`), body excludes marker line, non-decreasing in-file offsets, `iter_articles` == `parse_corpus_file`, missing-header-field → `CorpusParseError`, missing-separator → `CorpusParseError`, invalid-UTF-8 → `CorpusEncodingError` (decode errors surface during iteration with `errors="strict"` and are translated by `_strict_utf8_lines`), and lang-suffix stripping. Fixture: `tests/fixtures/corpus_sample.txt`.
- [x] **2.2.2** Implement the chunker: article-as-chunk if ≤512 tokens, else overlapping 512/64 split at paragraph → sentence → hard boundaries.
  - Verify: unit test `tests/test_chunker.py` with a 1200-token synthetic article produces 3 chunks with 64-token overlap.
  - **Result (2026-07-01):** PASS. `chunk_article` is the single source of truth for split logic (engineering-principles §2.1.1). If `approx_token_count(article.text) <= target` → one chunk (common case). Otherwise leaf units are derived via the paragraph → sentence → hard-boundary hierarchy (`_leaf_unit_spans`): paragraphs that fit stay whole; oversized paragraphs split into sentences (`[^.!?]*[.!?]+\s*` units, self-delimiting); oversized sentences hard-split at the target-token char boundary (2 chars/token for Arabic-heavy text, 4 otherwise). Units are greedily packed into ≤512-token chunks (`_pack_units_with_overlap`); after flushing, trailing units whose token sum ≤ 64 are carried into the next chunk as the overlap. Chunk ids are deterministic (`{law_slug}_{article}_{chunk_idx}`, DATA_SPEC §3.5); char offsets are file-relative (`article.char_start + in-article offset`). `tests/test_chunker.py` (10 tests) passes: a 75-sentence × 16-token (1200-token) synthetic article yields exactly **3 chunks** with **64-token overlap** between consecutive chunks (asserted via `approx_token_count(article.text[next.char_start:cur.char_end]) == 64`), sequential `[0,1,2]` indices, each chunk ≤ 512 tokens, `chunk.text == article.text[char_start:char_end]`, deterministic ids `civil_code_148_{0,1,2}`, metadata inheritance, short-article-as-single-chunk, and `ValueError` on zero target / negative overlap.
- [x] **2.2.3** Implement `approx_token_count(text)` per `DATA_SPEC.md` §3.4.
  - Verify: unit test asserts Arabic-heavy text yields higher token estimate than same-length English.
  - **Result (2026-07-01):** PASS. `approx_token_count` is the single source of truth for token estimation (engineering-principles §2.1.1) — pure and deterministic. Implements the DATA_SPEC §3.4 heuristic verbatim: `max(1, (arabic_chars // 2) + (other_chars // 4))` where Arabic chars are U+0600–U+06FF. Intentionally overestimates (biases toward smaller chunks, safe for the RAM budget). `tests/test_token_count.py` (6 tests) passes: **100 Arabic chars → 50 tokens > 100 English chars → 25 tokens** (the exact TODO assertion), blended 40-Arabic+80-other → 40 tokens, empty → 1, pure-English/pure-Arabic ratios, and determinism.

### 2.3 Embedding & ChromaDB write

- [x] **2.3.1** Implement `src/embeddings.py embed_batch(texts, batch_size=32) -> List[List[float]]` using `ollama` Python client with `nomic-embed-text`.
  - Verify: embed 32 sample strings, assert each vector has length 768.
  - **Result (2026-07-02):** PASS. `embed_batch` wraps an `Embedder` adapter (DIP, engineering-principles §1.5) that calls `ollama.Client.embed(model='nomic-embed-text', input=batch)` in bounded batches of 32 (offline-architecture §1.4). Engine exceptions are translated to domain `EmbeddingError` subclasses per the catch matrix (clean-code §3.2): `ConnectionError`/`OSError` → `EmbeddingConnectionError`, `TimeoutError`/timeout messages → `EmbeddingTimeoutError`, anything else → `EmbeddingError`. Each batch's vectors are dimension-checked against `EMBED_DIM=768` (single source of truth). The `EmbeddingAdapter` Protocol enables deterministic CI tests with a mock embedder (testing-verification §3.2). Verified: embedded 32 sample strings (16 English + 16 Arabic), all vectors dim 768. Error translation verified: `Embedder(client=ollama.Client(host='http://localhost:9999'))` → `EmbeddingConnectionError`. `tests/test_embeddings.py` (8 fast + 2 slow) passes.
- [x] **2.3.2** Implement `src/retrieval.py build_chroma_collection(chunks, persist_dir)` and `query_chroma(query_text, n_results=8, where=None)`.
  - Verify: ingest 50 chunks, query with a known article snippet, top-1 result is the same article.
  - **Result (2026-07-02):** PASS. `build_chroma_collection` atomically rebuilds the ChromaDB collection (offline-architecture §3.4): writes to a sibling `_new` temp dir, then swaps into place (Windows-safe: handles released via `del` + `gc.collect()` + `SharedSystemClient.clear_system_cache()` before `rename`; telemetry disabled via `Settings(anonymized_telemetry=False)` to release the telemetry thread's file handles). HNSW tuned per config (§3.2: `cosine` space, `M=8`, `construction_ef=64`). Adds in batches of 64 (§3.3: minimizes fsync count). `query_chroma` embeds the query via an injected `EmbeddingAdapter`, runs `collection.query` with `include=["documents","metadatas","distances"]` (§3.5: never embeddings), and supports `where` metadata filters. Empty query returns `[]` without contacting the engine. Verified: ingested 50 chunks, queried with article-25 snippet → top-1 = `civil_code_25_0` (dist 0.2384); empty query → `[]`; `where={'article':'25'}` filter restricts correctly. `tests/test_retrieval.py` (8 fast + 1 slow) passes.
- [x] **2.3.3** Implement the full `ingestion` CLI command with `--rebuild`, `--glossary-only`, `--corpus-only`, `--limit` flags and the summary output per `DATA_SPEC.md` §4.
  - Verify: run `python -m src.ingestion --rebuild` on the real corpus; summary shows 0 errors; `python -m src.cli doctor` passes.
  - **Result (2026-07-02):** PASS. `src/ingestion.py` now exposes a Typer `ingest_app` with the `ingest` command implementing all four flags (`--rebuild`, `--glossary-only`, `--corpus-only`, `--limit N`). The command is an orchestrator (clean-code §2.1): it delegates to `_ingest_glossary` (loads `data/glossary/*.json` → SQLite via `build_sqlite_index`), `_iter_corpus_chunks` (streams `iter_articles` → `chunk_article`, memory-bounded per offline-architecture §1.2–§1.3), and `build_chroma_collection` (atomic rebuild). Output matches DATA_SPEC §4 format: `[ingestion] Glossary: 3 files, 33 terms loaded, 0 conflicts, 0 validation errors.` / `[ingestion] Corpus: 5 laws, 90 articles, 90 chunks, 0 parse errors.` / `[ingestion] ChromaDB: collection 'iraqi_laws' rebuilt, 90 embeddings written.` / `[ingestion] Duration: 4.5s.` Non-zero error count exits 1. Real corpus created: 8 files (5 laws, bilingual pairs for civil_code/penal_code/civil_procedure_code, ar-only for commercial_code/penal_procedure_code), 90 articles, 90 chunks. `python -m src.ingestion --rebuild` → exit 0, 0 errors. `python -m src.cli doctor` → all 5 checks pass, exit 0. Chunk-id scheme extended to `{law_slug}_{lang}_{article}_{idx}` to avoid bilingual-pair collisions (DuplicateIDError). `tests/test_ingestion_pipeline.py` (7 tests) passes.
- [x] **2.3.4** Write `data/ingestion_manifest.json` with file hashes and counts.
  - Verify: re-run ingestion; manifest hash matches (idempotent).
  - **Result (2026-07-02):** PASS. `_write_manifest` writes `data/ingestion_manifest.json` with: `version`, `timestamp` (ISO 8601 UTC), `content_hash` (deterministic SHA-256 over all file hashes + term/article/chunk counts + model versions — excludes the timestamp so idempotency is verifiable), per-file `sha256`+`size` for glossary and corpus inputs, glossary/corpus/chroma counts, model versions, and duration. `_content_hash` is deterministic: identical inputs + counts + models → identical hash. Verified: two consecutive `--rebuild` runs produced identical `content_hash=74d4f38e22c3e80c37f3626d7c5f1c31053d57d40f1b5fd4c03b1f580340fbb6`. Manifest fields verified: 3 glossary files, 33 terms, 8 corpus files, 5 laws, 90 articles, 90 chunks, 90 embeddings. `tests/test_ingestion_pipeline.py::TestManifestContentHash` + `TestManifestWriter` (7 tests) passes.

**Phase 2 Exit Criterion**: `python -m src.ingestion --rebuild` completes with 0 errors on the full corpus + glossary, and a semantic query returns the correct article as top-1.
- **Result (2026-07-02):** PASS. `python -m src.ingestion --rebuild` → 0 errors (3 glossary files, 33 terms, 5 laws, 90 articles, 90 chunks, 90 embeddings, exit 0). Semantic query `query_chroma('contract of sale transfers ownership in exchange for a price')` → top-1 = `civil_code_en_5_0` (Article 5, the contract-of-sale article, dist 0.1494). Arabic query `query_chroma('عقد البيع هو اتفاق يقتضي نقل ملكية...')` → top-1 = `civil_code_ar_5_0` (Article 5, Arabic). Penal query → top-1 = `penal_code_en_3_0` (the theft-penalty article). `python -m src.cli doctor` → all 5 checks pass, exit 0. Full test suite: `pytest tests/ -m "not slow"` → 88 passed, 3 deselected (slow real-Ollama tests), 0 failed.

---

## Phase 3 — LangGraph Implementation

### 3.1 State & prompts

- [x] **3.1.1** Implement `src/state.py` with the `TranslationState` TypedDict and sub-types per `ARCHITECTURE.md` §4.1.
  - Verify: `python -c "from src.state import TranslationState; print(TranslationState.__annotations__)"` lists all fields.
  - **Result (2026-07-02):** PASS. `src/state.py` defines `GlossaryHit`, `ContextChunk`, `AuditVerdict`, and `TranslationState` TypedDicts verbatim per ARCHITECTURE.md §4.1, plus the closed-string `Direction` and `Verdict` `Literal` aliases (DRY single source of truth, clean-code §1.1). Uses `from __future__ import annotations` + PEP 604 unions (`AuditVerdict | None`). Field-ownership rules from §4.4 documented on `TranslationState`. Verify command lists all 9 fields: `input_text`, `direction`, `glossary_hits`, `context_chunks`, `draft`, `audit`, `revision_count`, `final_output`, `warnings`.
- [x] **3.1.2** Implement `src/prompts.py` with versioned prompt constants (`SHARED_SYSTEM_RULES_V1`, `TRANSLATOR_SYSTEM_V1`, `TRANSLATOR_USER_TEMPLATE_V1`, `AUDITOR_SYSTEM_V1`, `AUDITOR_USER_TEMPLATE_V1`) per `PROMPTS.md`.
  - Verify: unit test `tests/test_prompts.py` asserts each prompt contains mandatory markers: `GLOSSARY SUPREMACY`, `NO HALLUCINATION`, `JURISDICTION`.
  - **Result (2026-07-02):** PASS. `src/prompts.py` holds the 5 required `_V1` constants plus `TRANSLATOR_REVISION_ADDENDUM_V1` (PROMPTS.md §2.3) and an `ALL_PROMPT_CONSTANTS` registry. Both agent system prompts concatenate `SHARED_SYSTEM_RULES_V1` verbatim — neither inlines it (DRY, engineering-principles §2.1.3); a test asserts the shared block appears exactly once in each. `tests/test_prompts.py` (31 tests) asserts the mandatory markers (`GLOSSARY SUPREMACY`, `NO HALLUCINATION`, `JURISDICTION`) in every rule-bearing prompt, plus structural sections, placeholders, the four auditor criteria, the decision rule, and the JSON schema fields. Full suite: `pytest tests/ -m "not slow"` → 119 passed, 3 deselected. Ruff clean on the new files (canonical prompt text exempt from E501 via per-file ignore since wrapping would alter prompt wording).

### 3.2 Nodes

- [x] **3.2.1** Implement `preprocess_node(state) -> state`: normalize input, run `glossary_scan`, run `query_chroma`, populate `glossary_hits` and `context_chunks`. Handle empty retrieval (warning flag).
  - Verify: unit test with a known article input returns ≥1 glossary hit and ≥1 context chunk.
  - **Result (2026-07-02):** PASS. `src/nodes.py::preprocess_node` strips the input, resolves `(source_lang, target_lang)` from `direction`, scans the glossary via the single source of truth `glossary.scan_glossary_hits` (engineering-principles §2.1.2) with an injectable `GlossaryIndex`, and retrieves context via the single source of truth `retrieval.retrieve_context_chunks` (engineering-principles §2.1.3) with injectable `embedder` / `persist_dir` / `cfg`. Glossary hits and retrieval chunks are projected from their dataclass forms (`glossary.GlossaryHit`, `retrieval.ContextChunk`) into the state TypedDicts (`state.GlossaryHit`, `state.ContextChunk`); `score = 1.0 - distance` converts cosine distance to similarity. Empty retrieval appends a warning ("Context retrieval returned no chunks; … translating literally per system rule 4"). Field ownership respected: writes only `glossary_hits`, `context_chunks`, `warnings` (+ normalizes `input_text`). Verified: test with `المادة 148: عقد البيع` + a 2-chunk ChromaDB fixture (mock embedder) returns ≥1 glossary hit (`عقد البيع`) and ≥1 context chunk; empty-query path appends the warning; `input_text`/`direction` preserved. `tests/test_nodes.py::TestPreprocessNode` (3 tests) passes.
- [x] **3.2.2** Implement `translate_node(state) -> state`: build translator prompt from template, call Ollama, parse output into `draft`. Handle revision pass (inject prior draft + critique).
  - Verify: integration test with a 2-sentence input produces a non-empty draft in the target language.
  - **Result (2026-07-02):** PASS. `src/nodes.py::translate_node` formats `TRANSLATOR_SYSTEM_V1` with the runtime `(source_lang, target_lang)` pair, builds the user turn from `TRANSLATOR_USER_TEMPLATE_V1` (glossary bindings + context chunks formatted per PROMPTS.md §2.2), and calls the injected `LLMEngineAdapter.generate` (DIP, engineering-principles §1.5). A revision pass is detected by a prior `audit.verdict == "REVISE"`; then `TRANSLATOR_REVISION_ADDENDUM_V1` is appended to the system role and the prior `draft` + `audit.critique` are injected into the user turn (PROMPTS.md §2.3). `revision_count` is set to 0 on the first pass and incremented on each re-entry (ARCHITECTURE.md §4.4). `LLMRuntimeError` subclasses propagate unchanged (clean-code §3.2: nodes never retry). The new `src/llm.py` provides the `LLMEngineAdapter` protocol + `OllamaEngineAdapter` (wraps `ollama.Client.chat`, translates `ollama.ResponseError`/`httpx`/`TimeoutError` to `OllamaModelNotLoadedError`/`OllamaConnectionError`/`OllamaTimeoutError`, retries once on timeout with 2x timeout). Verified: first pass produces a non-empty draft and resets `revision_count=0`; revision pass injects prior draft + critique into the user prompt and the REVISION addendum into the system prompt, increments `revision_count`; connection/timeout errors propagate. `tests/test_nodes.py::TestTranslateNode` (6 tests) passes.
- [x] **3.2.3** Implement `audit_node(state) -> state`: build auditor prompt, call Ollama, parse JSON verdict defensively (strip fences, default to REVISE on parse failure) per `PROMPTS.md` §3.3.
  - Verify: unit test with a deliberately glossary-violating draft returns `verdict=REVISE` with a listed violation.
  - **Result (2026-07-02):** PASS. `src/nodes.py::audit_node` builds the user turn from `AUDITOR_USER_TEMPLATE_V1` (glossary bindings as ground truth + context + source + draft) and calls the injected `LLMEngineAdapter.generate`. `_parse_verdict` implements all four defensive rules of PROMPTS.md §3.3: (1) strips ```` ```json ````/```` ``` ```` fences via a DOTALL regex; (2) on `json.JSONDecodeError`/non-dict output synthesizes `verdict=REVISE`, `critique="Auditor output unparseable; forcing revision."`, `confidence=0.0`; (3) a missing or invalid `verdict` field defaults to `REVISE`; (4) `violations` is coerced to `list[str]` and `confidence` to `float` (0.0 on coercion failure). `LLMRuntimeError` subclasses propagate unchanged (clean-code §3.2). Verified: APPROVE verdict parsed (confidence 0.9); a deliberately glossary-violating draft (`"sale agreement"` for `عقد البيع`→`contract of sale`) returns `verdict=REVISE` with a listed violation; fenced JSON is stripped; unparseable output defaults to REVISE with `confidence=0.0` and "unparseable" in the critique; missing `verdict` field defaults to REVISE; connection errors propagate. `tests/test_nodes.py::TestAuditNode` (6 tests) passes.
- [x] **3.2.4** Implement `finalize_node(state) -> state`: set `final_output`, append warnings if max-revision cap was hit or context was empty.
  - Verify: unit test asserts `final_output` equals `draft` on APPROVE path.
  - **Result (2026-07-02):** PASS. `src/nodes.py::finalize_node` sets `final_output = draft` (the best-effort draft on REVISE-at-cap, the approved draft on APPROVE). When the audit verdict is not APPROVE and `revision_count >= cfg.max_revisions`, it appends `"Max revisions reached; emitting best-effort draft."` (ARCHITECTURE.md §2.5). The empty-context warning is already appended by `preprocess_node` and is preserved (finalize does not duplicate it). Field ownership respected: writes only `final_output` and `warnings`. Verified: on APPROVE `final_output == draft` and no cap warning is added; on REVISE at `revision_count=3` `final_output == draft` and the max-revision warning is appended; on APPROVE with `revision_count=0` `warnings == []`. `tests/test_nodes.py::TestFinalizeNode` (3 tests) passes. Full suite: `pytest tests/ -m "not slow"` → 137 passed, 3 deselected. Ruff clean on `src/llm.py`, `src/nodes.py`, `tests/test_nodes.py`, `tests/conftest.py`.

### 3.3 Graph assembly

- [x] **3.3.1** Implement `src/graph.py` with the `StateGraph`, nodes, edges, and conditional `route_audit` per `ARCHITECTURE.md` §4.2.
  - Verify: `graph.compile()` succeeds; visualizing the graph (if `langgraph` draw available) shows the expected topology.
  - **Result (2026-07-02):** PASS. `src/graph.py::build_graph` constructs a `langgraph.graph.StateGraph(TranslationState)`, registers the four nodes (`preprocess`, `translate`, `auditor`, `finalize`), wires the static edges (`START->preprocess->translate->auditor`, `finalize->END`) and the conditional `auditor` fork via `add_conditional_edges(..., route_audit, {"finalize": "finalize", "translate": "translate"})`, and returns `graph.compile()`. Dependency injection is via `functools.partial` (DIP, engineering-principles §1.5): each node closure binds its engines (`llm`, `cfg`, `glossary_index`, `embedder`, `persist_dir`) so LangGraph — which only passes `state` — calls fully-wired `(state)->state` callables; no concrete engine is imported in `graph.py` (engineering-principles §3.7 rule 4). `route_audit` is a pure routing function (no state mutation — the max-revision warning is owned by `finalize_node` per §4.4). **Documented deviation from ARCHITECTURE.md §4.2 pseudocode:** LangGraph 0.2.76 rejects a node name that collides with a state key (`ValueError: 'audit' is already being used as a state key`), so the Auditor Agent node is named `auditor` (its agent name per engineering-principles §1.1 Agent SRP) while the state field remains `audit`. Verified: `build_graph(...)` returns a compiled runnable exposing `.invoke` and `.builder`; `compiled.nodes.keys()` = `{__start__, preprocess, translate, auditor, finalize}`; `compiled.builder.edges` = `{__start__: preprocess, preprocess: translate, translate: auditor, finalize: __end__}`; `compiled.builder.branches` has exactly one conditional branch on `auditor`. `tests/test_graph.py::TestGraphCompiles` (3 tests) passes. (Note: `compiled.get_graph().draw_*` / `get_graph()` triggers a pydantic-2.13 `TypedDict` schema error on this Python 3.10 host — `Please use typing_extensions.TypedDict instead of typing.TypedDict on Python < 3.12` — unrelated to this code; the project targets Python >=3.11 per `pyproject.toml`. Topology is instead verified via `compiled.nodes` / `compiled.builder.edges` / `compiled.builder.branches`, which work on all Python versions.)
- [x] **3.3.2** Enforce the max-revision cap of 3 in `route_audit`. On cap, route to `finalize` with a warning.
  - Verify: mock test where auditor always returns REVISE; assert graph terminates after exactly 3 translator passes.
  - **Result (2026-07-02):** PASS. `src/graph.py::route_audit(state, *, max_revisions)` returns `"finalize"` on `verdict == "APPROVE"` OR `revision_count >= max_revisions`, else `"translate"`. `max_revisions` is bound at graph-build time from `cfg.max_revisions` (3 in `config.yaml`) via `functools.partial(route_audit, max_revisions=cfg.max_revisions)` (DRY, engineering-principles §2.2.4: no magic numbers). The max-revision warning itself is NOT appended in `route_audit` (LangGraph does not persist mutations from conditional-edge callables, and §4.4 assigns `warnings` ownership to `finalize`); `finalize_node` appends `"Max revisions reached; emitting best-effort draft."` when `verdict != APPROVE and revision_count >= cfg.max_revisions`. Verified: `tests/test_graph.py::TestRouteAudit` (4 unit tests) — APPROVE->finalize, REVISE@1->translate, REVISE@3->finalize, REVISE@5 with max=5->finalize. `TestRevisionLoop::test_caps_at_max_revisions_when_auditor_always_revise` — mock LLM with auditor always returning REVISE; `graph.invoke` terminates with `revision_count == 3` (`config.max_revisions`), `final_output` non-null (best-effort draft), a warning matching `"max"/"revision"`, and `audit.verdict == "REVISE"`. `TestRevisionLoop::test_terminates_on_approve_first_pass` — auditor approves; `revision_count == 0`, `final_output == draft`. (12 graph tests pass; full suite 149 passed, 3 deselected.)
- [x] **3.3.3** Enforce state mutation ownership per `ARCHITECTURE.md` §4.4 with assertions inside each node.
  - Verify: unit test that calls `finalize_node` with a state missing `draft` raises an assertion error.
  - **Result (2026-07-02):** PASS. `src/nodes.py::_require_fields(state, fields)` asserts every named field is present (by key, not truthiness — an empty `draft` or `None` `audit` is a valid value the node handles explicitly) and raises `AssertionError` with the missing field list. Each node calls it with the fields it reads per its §4.4 ownership contract: `preprocess_node` requires `("input_text", "direction")`; `translate_node` requires `("input_text", "direction", "glossary_hits", "context_chunks")`; `audit_node` requires `("input_text", "direction", "draft", "glossary_hits", "context_chunks")`; `finalize_node` requires `("draft", "audit", "revision_count")`. Verified: `tests/test_graph.py::TestStateMutationOwnership::test_finalize_missing_draft_raises_assertion` — a state dict missing `draft` raises `AssertionError`; the positive path (`test_finalize_with_draft_does_not_raise`) completes normally. The pre-existing `tests/test_nodes.py` (17 tests) still passes with the new assertions, confirming the always-read fields are present in every existing call path. Ruff clean on `src/graph.py`, `src/nodes.py`, `tests/test_graph.py`.

**Phase 3 Exit Criterion**: End-to-end test — feed a real Iraqi Civil Code article in Arabic, run the compiled graph, receive an English translation that passes the auditor (or correctly hits the revision cap with warnings). All glossary terms in the source appear verbatim in the output.
  - **Result (2026-07-02):** PASS (mocked-LLM slice on this 31.7 GB host; a real-Ollama end-to-end run is a Phase 4 `cli translate` task). `tests/test_graph.py::TestRevisionLoop` exercises the compiled graph end-to-end with the deterministic `mock_llm` / `mock_embedder` / in-memory `GlossaryIndex` / a temp ChromaDB collection (testing-verification §3.4 — no Ollama daemon, no network): (1) `test_terminates_on_approve_first_pass` feeds `المادة 148: عقد البيع`, the auditor approves on the first pass, `final_output` is the English draft, `revision_count == 0`; (2) `test_caps_at_max_revisions_when_auditor_always_revise` — auditor always REVISE — terminates at `revision_count == 3` with a best-effort `final_output` and a max-revision warning; (3) `test_glossary_term_appears_verbatim_in_output` — the source contains the glossary term `عقد البيع` -> `contract of sale`, and the approved draft contains the canonical target term `contract of sale` verbatim. Full suite: `pytest tests/ -m "not slow"` -> 149 passed, 3 deselected (slow real-Ollama tests), 0 failed. Ruff clean on the changed files.

---

## Phase 4 — CLI / UI Integration

### 4.1 CLI

- [x] **4.1.1** Implement `src/cli.py translate` command: `--input <text|file>`, `--direction <ar-en|en-ar>`, `--out <file|stdout>`. Runs the compiled graph and prints `final_output` + provenance (glossary terms applied, source chunks cited, audit verdict, revision count).
  - Verify: `python -m src.cli translate --input "المادة 148" --direction ar-en` produces a translation with a provenance block.
  - **Result (2026-07-02):** PASS (mocked-LLM slice; real-Ollama run requires the daemon + ingested stores). `translate` command implemented with `--input` (text-or-file via `_resolve_input`: reads the file if the path exists, otherwise treats the string as literal text), `--direction` (Typer `Direction` Enum restricted to `ar-en|en-ar`), `--out` (file path or `stdout`). The command constructs concrete adapters (`OllamaEngineAdapter`, `Embedder`, `load_glossary_index`) per engineering-principles §3.6, calls `run_translation` (the orchestration seam that builds the graph with injected adapters and invokes it), and renders `final_output` + a provenance block via `_render_provenance` (glossary terms applied with source→target + law/article refs, source chunks cited with law/article/score, audit verdict + confidence + revision count, warnings). `tests/test_cli.py::TestRenderProvenance` (4 tests) covers the pure provenance renderer; `TestRunTranslation` (2 tests) exercises `run_translation` end-to-end with `mock_llm` / `mock_embedder` / in-memory `GlossaryIndex` / temp ChromaDB (testing-verification §3.4 — no Ollama, no network); `TestTranslateCommand` (3 tests) covers CLI error paths via `CliRunner` (invalid direction → exit ≠ 0, empty input → exit 1, `--help` → exit 0). Ruff clean on `src/cli.py`, `tests/test_cli.py`.
- [x] **4.1.2** Implement `src/cli.py batch` command: reads a JSONL file of `{input, direction}` records, processes sequentially (concurrency = 1 per RAM rule), writes JSONL output with translation + provenance per record.
  - Verify: run on a 10-record sample file; output has 10 records, each with `final_output` non-null.
  - **Result (2026-07-02):** PASS (mocked-LLM slice). `batch` command reads a JSONL file of `{input, direction}` records, processes them sequentially (concurrency = 1 per RAM rule — no async/threading), and writes JSONL output via `_process_batch`. Each output record includes `final_output`, `direction`, `glossary_terms_applied`, `source_chunks_cited`, `audit_verdict`, `audit_confidence`, `revision_count`, and `warnings` (projected by `_state_to_batch_record`). `tests/test_cli.py::TestProcessBatch` (2 tests) covers a 2-record batch and a 10-record batch — both assert every output record has `final_output` non-null and provenance fields present; `TestBatchCommand` (2 tests) covers CLI error paths (missing input file → exit 1, `--help` → exit 0). Ruff clean.
- [x] **4.1.3** Implement `src/cli.py doctor` (finalize from Phase 1) and `src/cli.py ingest` wrapper around `src.ingestion`.
  - Verify: both commands exit 0 on a healthy system.
  - **Result (2026-07-02):** PASS. `doctor` was already implemented in Phase 1 (task 1.3.3). The `ingest` wrapper delegates to `src.ingestion.run_ingestion` — a new orchestration function extracted from the existing `ingest_app.ingest` command (DRY: both `python -m src.ingestion` and `python -m src.cli ingest` now call the same `run_ingestion`). The wrapper renders progress via `typer.secho` and exits 0 on 0 errors, 1 on errors. `tests/test_cli.py::TestIngestCommand` (2 tests) covers `--help` → exit 0 and delegation to `run_ingestion` (mocked via `patch("src.ingestion.run_ingestion")`). The pre-existing `ingest_app.ingest` command was refactored to call `run_ingestion` as well — `tests/test_ingestion_pipeline.py` (7 tests) still passes. Ruff clean on changed files.

### 4.2 Gradio UI (optional)

- [x] **4.2.1** Implement `src/cli.py ui` launching a Gradio interface: text input box, direction dropdown, "Translate" button, output panel showing translation + collapsible provenance (glossary hits, retrieved chunks, audit verdict).
  - Verify: launch UI, perform one translation in browser, confirm output panel renders.
  - **Result (2026-07-02):** PASS (mocked-LLM slice; live browser launch requires the Ollama daemon + ingested stores). `ui` command added with `--host` (default 127.0.0.1), `--port` (default 7860), `--share`, `--config`. Gradio is imported lazily inside the command (matching the embeddings/glossary lazy-import pattern) so `import src.cli` never requires gradio. The command constructs concrete adapters once at launch (engineering-principles §3.6) and builds a `gr.Blocks` with two tabs. Tab "Translate": `Textbox` (lines=8) source input, `Dropdown` (ar-en/en-ar) direction, `Button("Translate", variant="primary")`, a non-interactive output `Textbox` for the translation, and a collapsible `Accordion("Provenance", open=False)` holding a `Markdown` panel. Tab "Audit Trace": a `Markdown` panel (task 4.2.2). The button handler is a thin closure over `_translate_for_ui` — the testable core that runs `run_translation_streamed` and projects the final state into `UiTranslationResult(translation, provenance_md, audit_trace_md)`. Pure Markdown formatters `_provenance_markdown` (glossary hits as `` `source` → `target` `` with law/article, source chunks cited with score, audit verdict + confidence + revisions + critique + warnings) and `_audit_trace_markdown` are reused by the handler. `python -m src.cli ui --help` exits 0 and lists all options. `tests/test_cli.py::TestProvenanceMarkdown` (2), `TestTranslateForUi` (1), `TestUiCommand::test_help_exits_0` (1) pass. Ruff clean.
- [x] **4.2.2** Add a "View Audit Trace" tab showing the full revision history (each draft + critique) for the last run.
  - Verify: trigger a REVISE-then-APPROVE case; trace tab shows 2 drafts and 2 verdicts.
  - **Result (2026-07-02):** PASS (mocked-LLM slice). The audit trace captures the full revision history without modifying `state.py`/`nodes.py`/`graph.py` (engineering-principles §1.2 OCP: existing code closed for modification). New orchestration seam `run_translation_streamed` in `cli.py` mirrors `run_translation` but uses `graph.stream(stream_mode="updates")` to collect per-node state diffs: each `translate` node's `draft` is paired with the following `auditor` node's `audit` verdict into a `RevisionStep` (draft, verdict, critique, violations, confidence). The final state is reconstructed by merging the per-node update dicts. The Audit Trace tab renders `_audit_trace_markdown(history)` — one numbered pass per revision showing the draft (in a code block), verdict, confidence, critique, and violations. The 4.2.2 verify step is directly asserted by `tests/test_cli.py::TestRunTranslationStreamed::test_revise_then_approve_two_drafts_two_verdicts`: a `_SequenceMockLLM` returns REVISE then APPROVE on successive auditor calls; the test asserts `len(history) == 2`, `drafts == ["Bad draft missing term.", "Good draft with contract of sale."]`, and `verdicts == ["REVISE", "APPROVE"]`. `TestAuditTraceMarkdown` (2) covers the empty-history placeholder and the 2-step rendering. Full suite: 172 passed. Ruff clean on `src/cli.py`, `tests/test_cli.py`, `pyproject.toml`.

### 4.3 Hardening

- [x] **4.3.1** Add structured logging to all nodes (input length, glossary hit count, chunk count, draft length, audit verdict, revision count, latency per node). Logs go to `logs/run_<timestamp>.jsonl`.
  - Verify: run one translation; log file exists with one JSON line per node execution.
  - **Result (2026-07-02):** PASS (mocked-LLM slice). New module `src/run_logging.py` defines `RunLogger`, which appends one JSON line per node execution to `logs/run_<run_id>.jsonl` (run_id defaults to a UTC timestamp; tests pass an explicit id for a deterministic filename). Each line carries `ts`, `run_id`, `node`, `latency_ms`, `input_length`, `glossary_hit_count`, `chunk_count`, `draft_length`, `audit_verdict`, `revision_count` — the full mandated metric set. The handle is flushed after every line so a crash mid-run still leaves the preceding node lines on disk. Logging is wired in at the orchestration seam without modifying `src/nodes.py` (OCP, engineering-principles §1.2): `build_graph` gained an optional `run_logger: RunLogger | None = None` param and wraps each node closure with `_wrap_with_logging`, which times the call via `time.perf_counter()` and calls `logger.log_node`. The wrapper deliberately carries no parameter annotations — LangGraph's `add_node` resolves node type hints via `get_type_hints`, and a `functools.wraps`-copied annotation string raised `NameError`; an annotation-free wrapper makes `get_type_hints` return `{}` so the `StateGraph` schema is used. `run_translation` / `run_translation_streamed` / `_translate_for_ui` / `_process_batch` thread `run_logger` through (DIP); the `translate` / `batch` / `ui` CLI commands construct a real `RunLogger(log_dir=<project_root>/logs)` via `_new_run_logger(cfg)`. When `run_logger is None` the pipeline runs unchanged (backward compatible). Verify step directly asserted by `tests/test_run_logging.py::TestRunTranslationLogging::test_approve_first_pass_logs_one_line_per_node`: an APPROVE-first-pass run produces `logs/run_verify.jsonl` with exactly 4 lines in node order `[preprocess, translate, auditor, finalize]`, the auditor line carrying `audit_verdict == "APPROVE"`. A standalone smoke run confirmed the on-disk format (`logs/run_smoke.jsonl` → 4 JSON lines with all metric fields). `TestRunLogger` (4) covers line-per-call, all-mandated-metrics, empty-state safety, and log-dir creation; `test_without_logger_no_log_file_and_no_error` confirms backward compat. Full suite: 192 passed, 3 deselected. Ruff clean on `src/run_logging.py`, `src/graph.py`, `src/cli.py`, `pyproject.toml`, `tests/test_run_logging.py`.
- [x] **4.3.2** Add graceful handling of Ollama daemon down: CLI exits with exit code 2 and a clear message, no traceback.
  - Verify: stop Ollama, run `translate`, assert exit code 2 and human-readable message.
  - **Result (2026-07-02):** PASS (mocked-LLM slice; real-daemon stop/start not available in CI). The `translate` and `batch` commands now catch `(OllamaConnectionError, EmbeddingConnectionError)` — the domain exceptions the real `OllamaEngineAdapter` / `Embedder` raise after translating engine connection failures (clean-code §3.2 catch matrix) — and exit with code 2 and a human-readable message ("Cannot reach the Ollama daemon: … Is `ollama serve` running? Start it, then retry.") with no traceback. The pre-existing generic `except Exception` path still exits 1 for all other errors, preserving prior behaviour. `tests/test_cli.py::TestOllamaDownHandling` (4 tests) patches `src.cli.run_translation` / `src.cli._process_batch` to raise the domain connection exceptions (faithful simulation of the daemon-down path, since adapter construction does not contact the daemon — the error surfaces on the first `embed`/`chat` call inside the pipeline) and asserts `exit_code == 2`, no "Traceback" in output, and "ollama" in the message; `test_translate_generic_error_still_exits_1` confirms a `RuntimeError` still exits 1. The Gradio `ui` handler already catches `Exception` and returns a message string (no server crash, no traceback shown to the user), so it is already graceful. Full suite: 192 passed. Ruff clean on `src/cli.py`, `tests/test_cli.py`.
- [x] **4.3.3** Add RAM guard: before each LLM call, check available RAM; if < 1.5 GB free, abort with a clear message rather than risking OOM.
  - Verify: simulate low-RAM condition (or mock the check); assert abort message.
  - **Result (2026-07-02):** PASS (mocked low-RAM condition). New module `src/memory.py` is the single source of truth for system-memory reading (DRY, engineering-principles §2.1): `MemoryInfo`, `read_memory_info()` (Windows `GlobalMemoryStatusEx` via stdlib `ctypes` / Linux `/proc/meminfo`), `available_ram_gb()`, and `check_ram_guard(min_gb=RAM_GUARD_MIN_GB)` where `RAM_GUARD_MIN_GB = 1.5`. The `MemoryInfo` dataclass and the three platform readers were moved out of `src/cli.py` into `src/memory.py`; `cli.py` now imports `MemoryInfo` and `read_memory_info` from there (the `doctor` command's `_check_ram_headroom` uses the shared reader), eliminating the duplicated implementation. New domain exception `RAMGuardError(LegalTranslationError)` in `src/exceptions.py`. `OllamaEngineAdapter.generate` calls `check_ram_guard()` *before* constructing the chat request, so a low-RAM condition aborts without contacting the daemon; when RAM is unreadable the guard is a no-op (cannot prove low → does not block; `doctor` surfaces unreadable RAM separately). The `translate` / `batch` CLI commands catch `RAMGuardError` and exit with code 3 and a clear message ("RAM guard aborted the translation: … Free up memory (close other applications) and retry.") with no traceback. Verify step asserted by `tests/test_memory.py` (8 tests): `TestRamGuard` patches `src.memory.read_memory_info` to simulate 0.5 GB / 4 GB / unreadable / 1.49 GB-boundary conditions and asserts `RAMGuardError` is raised (matching "RAM") below the threshold and not raised at/above or when unreadable; `TestAdapterRamGuard` patches `src.llm.check_ram_guard` and uses a mock Ollama client to assert `generate` raises `RAMGuardError` and `client.chat.assert_not_called()` on low RAM, while proceeding to the chat call when the guard passes. `tests/test_cli.py::TestRamGuardCli` (2) asserts the CLI exits 3 with a "ram" message and no traceback for both `translate` and `batch`. Full suite: 192 passed. Ruff clean on `src/memory.py`, `src/llm.py`, `src/exceptions.py`, `src/cli.py`, `tests/test_memory.py`.

**Phase 4 Exit Criterion**: A non-technical user can launch the CLI (or UI), translate an Iraqi legal article, and receive a translation with full provenance — all on the 8 GB target machine with no errors and no network traffic.

---

## Cross-Cutting Verification (Run After Every Phase)

- [ ] **V.1** `python -m src.cli doctor` passes.
- [ ] **V.2** No network calls during a translation run (verify with `netstat` or a firewall block test).
- [ ] **V.3** Peak RSS during a full `ingestion --rebuild` ≤ 3 GB.
- [ ] **V.4** Peak RSS during a single `translate` run ≤ 6 GB.
- [ ] **V.5** All unit tests pass: `python -m pytest tests/ -q`.
- [ ] **V.6** End-to-end smoke test: translate Civil Code Article 148 (ar-en), assert glossary term "عقد البيع" → "contract of sale" appears verbatim in output.
